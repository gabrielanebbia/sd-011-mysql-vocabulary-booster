-- SELECT job.JOB_TITLE AS 'Cargo', AVG(emp.SALARY) AS 'Média salarial'
-- FROM hr.jobs AS job
-- INNER JOIN employees AS emp ON emp.JOB_ID = job.JOB_ID
-- CASE
-- WHEN ROUND(AVG(emp.SALARY), 2) BETWEEN 2000 AND 5800 THEN 'Júnior'
-- WHEN ROUND(AVG(emp.SALARY), 2) BETWEEN 5801 AND 7500 THEN 'Pleno'
-- WHEN ROUND(AVG(emp.SALARY), 2) BETWEEN 7501 AND 10500 THEN 'Sênio'
-- WHEN ROUND(AVG(emp.SALARY), 2) > 10500 THEN 'CEO'
-- END AS 'Senioridade'
-- ORDER BY ROUND((MAX_SALARY + MIN_SALARY)/2, 2), JOB_TITLE;
