SELECT c1.ContactName AS `Nome`, c1.Country AS `País`, c2.`Count` AS `Número de compatriotas` FROM w3schools.customers AS c1 INNER JOIN ( SELECT Country, (COUNT(*) - 1) AS `Count` FROM w3schools.customers GROUP BY Country HAVING `Count` > 0 ) AS c2 ON c1.Country = c2.Country ORDER BY Nome;
